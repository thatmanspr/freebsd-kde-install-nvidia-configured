# freebsd-kde-install-nvidia-configured
FreeBSD KDE Plasma installer with correct NVIDIA driver configuration — fixes kernel panic on first boot, with Fish, Alacritty, and sane defaults out of the box.
